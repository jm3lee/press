## Analytics demo walkthrough

The detailed walkthrough now lives in the
[`analytics-stack` reference](/docs/reference/analytics-stack/), alongside
expanded coverage of `EngagementProvider` and `AutoTrack`. Use that guide to see
how the playground emits events, how the ingestion API stores them, and how to
adapt the patterns for production sites.

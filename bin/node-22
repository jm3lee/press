#!/bin/bash -x
entrypoint="$1"; shift
docker compose run \
    --build \
    --entrypoint "$entrypoint" \
    --rm -i -t \
    -u $(id -u) \
    -v ./app:/app \
    --workdir /app/flashoffer \
    node-22 "$@"

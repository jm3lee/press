#!/bin/bash -x
entrypoint="$1"; shift
docker run \
    --entrypoint "$entrypoint" \
    --rm -i -t \
    -u $(id -u) \
    -v /home/brian/press/app/flashoffer:/workspace \
    --workdir /workspace \
    --network host \
    node:22-slim "$@"

#!/bin/bash -x
entrypoint="$1"; shift
docker compose run \
    --build \
    --entrypoint "$entrypoint" \
    --rm -i -t \
    -u $(id -u) \
    -v /home/brian/press/app/flashoffer:/workspace \
    --workdir /workspace \
    node-22 "$@"
